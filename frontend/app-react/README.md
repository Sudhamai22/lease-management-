# React Frontend (Vite)

This folder contains a minimal React + Vite scaffold for role-based dashboards.

Run locally:

```bash
cd frontend/app-react
npm install
npm run dev
```

Open the URL shown by Vite (usually http://localhost:5173).
